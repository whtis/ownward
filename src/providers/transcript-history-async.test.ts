import{expect,test}from"bun:test";import{readClaudeTranscriptAsync,setTranscriptHistoryWorkerFactoryForTest}from"./transcript-history-async.ts";
test("Worker construction failures always release concurrency permits",async()=>{setTranscriptHistoryWorkerFactoryForTest(()=>{throw new Error("constructor failed")});try{const settled=Promise.allSettled([readClaudeTranscriptAsync("a"),readClaudeTranscriptAsync("b"),readClaudeTranscriptAsync("c"),readClaudeTranscriptAsync("d")]);const result=await Promise.race([settled,Bun.sleep(250).then(()=>null)]);expect(result).not.toBeNull();expect(result!.every(x=>x.status==="rejected")).toBeTrue();}finally{setTranscriptHistoryWorkerFactoryForTest(null);}});
