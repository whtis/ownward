import {expect,test} from "bun:test";import{parseRunnerHealth,REQUIRED_RUNNER_CAPABILITIES}from"./health-contract.ts";
const valid=()=>({ok:true,pid:1,runnerApiVersion:1,capabilities:[...REQUIRED_RUNNER_CAPABILITIES],buildIdentity:"a".repeat(64),draining:false,activeRuns:[],providers:[{id:"codex",state:"ready",errorClass:null}]});
test("strict Runner health gates API capability build and providers",()=>{expect(parseRunnerHealth(valid(),{requiredProviders:["codex"]}).pid).toBe(1);for(const mutate of[(x:any)=>x.runnerApiVersion=2,(x:any)=>x.capabilities=[], (x:any)=>x.buildIdentity="dev",(x:any)=>x.providers=[{id:"x",state:"maybe"}]]){const x=valid();mutate(x);expect(()=>parseRunnerHealth(x)).toThrow();}expect(()=>parseRunnerHealth(valid(),{expectedBuild:"b".repeat(64)})).toThrow("build identity");expect(()=>parseRunnerHealth(valid(),{requiredProviders:["claude"]})).toThrow("missing");const degraded=valid();degraded.providers[0]!.state="degraded";expect(()=>parseRunnerHealth(degraded,{requiredProviders:["codex"]})).toThrow("degraded");});
