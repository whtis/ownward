import{describe,expect,test}from"bun:test";import{mkdtempSync,rmSync}from"fs";import{join}from"path";import{tmpdir}from"os";import{RunnerServer,type RunnerProvider}from"./server.ts";
describe("Runner Provider health DTO",()=>{test("all registered providers expose the same explicit applicability schema",()=>{const root=mkdtempSync(join(tmpdir(),"ownward-provider-health-"));try{const provider:RunnerProvider={id:"fixture",version:"2.3.4",capabilities:new Set(["resume","stream"]),getMetrics:()=>({dropped:2}),async*execute(){}};const server=new RunnerServer(root,()=>provider);server.registerProvider(provider);expect(server.providerHealth()).toEqual([{id:"fixture",version:"2.3.4",state:"ready",lastSuccessAt:null,lastFailureAt:null,errorClass:null,queueDepth:{value:null,applicable:false,reason:"provider-executes-directly"},activeDepth:{value:0,applicable:true},capabilities:["resume","stream"],metrics:{dropped:2}}]);}finally{rmSync(root,{recursive:true,force:true})}});});
